server:
  port: 8083

spring:
  application:
    name: tradex-registration

tradex:
  registration:
    clients:
      auth:
        base-url: ${TRADEX_AUTH_BASE_URL:http://localhost:8081}
      member:
        base-url: ${TRADEX_MEMBER_BASE_URL:http://localhost:8082}
